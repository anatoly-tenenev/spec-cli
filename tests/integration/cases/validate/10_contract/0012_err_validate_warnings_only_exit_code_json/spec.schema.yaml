version: "0.0.7"
entity:
  doc:
    idPrefix: DOC
    pathTemplate: "entity.md"
    meta:
      fields:
        status:
          required: "${slug == 'never-required'}"
          schema:
            type: string
            const: approved
    content:
      sections:
        goal: {}
