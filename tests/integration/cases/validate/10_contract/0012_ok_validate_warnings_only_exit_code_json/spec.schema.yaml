version: "0.0.4"
entity:
  doc:
    idPrefix: DOC
    pathTemplate: "entity.md"
    meta:
      fields:
        status:
          required_when:
            eq?: [meta.slug, "never-required"]
          schema:
            type: string
            const: approved
    content:
      sections:
        goal: {}
