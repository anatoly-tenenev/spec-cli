version: "0.0.4"
entity:
  doc:
    id_prefix: DOC
    path_pattern: "entity.md"
    meta:
      fields:
        status:
          required_when:
            eq?: [meta.slug, "never-required"]
          schema:
            type: string
            const: approved
    content:
      sections:
        goal: {}
