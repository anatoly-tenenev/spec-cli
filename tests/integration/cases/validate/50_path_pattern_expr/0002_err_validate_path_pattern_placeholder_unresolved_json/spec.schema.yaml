version: "0.0.3"
entity:
  doc:
    id_prefix: DOC
    path_pattern: "{meta.owner}/{slug}.md"
    meta:
      fields:
        owner:
          required: false
          schema:
            type: string
            enum:
              - core
              - platform
    content:
      sections:
        goal: {}
