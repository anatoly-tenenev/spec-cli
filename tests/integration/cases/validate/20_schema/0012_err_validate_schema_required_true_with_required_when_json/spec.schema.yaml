version: "0.0.7"
entity:
  doc:
    idPrefix: DOC
    pathTemplate: "docs/${slug}.md"
    meta:
      fields:
        owner:
          required: "${`null`}"
          schema:
            type: string
    content:
      sections:
        goal: {}
