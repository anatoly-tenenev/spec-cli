version: "0.0.7"
entity:
  doc:
    idPrefix: DOC
    pathTemplate:
      cases:
        - use: "docs/${slug}.md"
        - when: true
          use: "draft/${slug}.md"
    content:
      sections:
        goal: {}
