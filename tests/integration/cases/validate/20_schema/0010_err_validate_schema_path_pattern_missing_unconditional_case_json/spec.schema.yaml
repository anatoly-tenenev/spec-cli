version: "0.0.3"
entity:
  doc:
    id_prefix: DOC
    path_pattern:
      cases:
        - when: true
          use: "docs/{slug}.md"
        - when: false
          use: "draft/{slug}.md"
    content:
      sections:
        goal: {}
