version: "0.0.3"
entity:
  doc:
    id_prefix: DOC
    path_pattern:
      cases:
        - use: "docs/{slug}.md"
        - when: true
          use: "draft/{slug}.md"
    content:
      sections:
        goal: {}
