version: 0.0.4
entity:
  doc:
    idPrefix: "DOC"
    pathTemplate: "${slug}.md"
    meta:
      fields:
        owner:
          required: "${xor(meta.slug, 'core')}"
          schema:
            type: string
            enum: [core, edge]
