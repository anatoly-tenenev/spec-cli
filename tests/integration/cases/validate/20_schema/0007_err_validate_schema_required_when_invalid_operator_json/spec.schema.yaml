version: 0.0.2
entity:
  doc:
    id_prefix: "DOC"
    path_pattern: "{slug}.md"
    meta:
      fields:
        - name: owner
          required: false
          required_when:
            xor: [meta.slug, core]
          schema:
            type: string
            enum: [core, edge]
