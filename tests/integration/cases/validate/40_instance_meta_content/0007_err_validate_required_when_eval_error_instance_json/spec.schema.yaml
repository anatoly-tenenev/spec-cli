version: "0.0.4"
entity:
  doc:
    idPrefix: DOC
    pathTemplate: "docs/{slug}.md"
    meta:
      fields:
        owner:
          required: false
          required_when:
            eq: [meta.createdDate, "2026-03-01"]
          schema:
            type: string
    content:
      sections:
        goal: {}
