version: "0.0.4"
entity:
  doc:
    idPrefix: DOC
    pathTemplate: "docs/${slug}.md"
    meta:
      fields:
        owner:
          required: "${length(meta.owner) > `0`}"
          schema:
            type: string
    content:
      sections:
        goal: {}
