version: "0.0.3"
entity:
  doc:
    id_prefix: DOC
    path_pattern: "entity.md"
    meta:
      fields:
        - name: status
          required_when:
            eq?: [meta.slug, "never-required"]
          schema:
            type: string
            const: approved
    content:
      sections:
        - name: goal
