version: "0.0.7"
entity:
  doc:
    idPrefix: DOC
    pathTemplate: "${meta.owner}/${slug}.md"
    meta:
      fields:
        owner:
          required: false
          schema:
            type: string
            enum:
              - core
              - platform
    content:
      sections:
        goal: {}
