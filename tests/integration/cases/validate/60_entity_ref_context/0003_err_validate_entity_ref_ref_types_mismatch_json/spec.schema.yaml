version: "0.0.3"
entity:
  service:
    id_prefix: SRV
    path_pattern: "services/{slug}.md"
    content:
      sections:
        - name: goal
  domain:
    id_prefix: DOM
    path_pattern: "domains/{slug}.md"
    content:
      sections:
        - name: goal
  feature:
    id_prefix: FEAT
    path_pattern: "features/{slug}.md"
    meta:
      fields:
        - name: owner
          required: false
          schema:
            type: entity_ref
            refTypes: [service]
    content:
      sections:
        - name: goal
