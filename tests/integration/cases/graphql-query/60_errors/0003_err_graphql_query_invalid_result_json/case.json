{
  "id": "graphql-query_60_0003_err_graphql_query_invalid_result_json",
  "description": "graphql-query fails with INVALID_QUERY_RESULT when selected non-null data is missing at runtime",
  "command": "graphql-query",
  "args": [
    "--workspace",
    "${WORKSPACE}",
    "--schema",
    "${SCHEMA}",
    "graphql-query",
    "--file",
    "${CASE}/query.graphql"
  ],
  "expect": {
    "exit_code": 1,
    "response_file": "response.json"
  },
  "workspace": {
    "input_dir": "workspace.in",
    "output_dir": "workspace.out",
    "assert_output": false
  }
}
