query {
  feature(where: { slug: { eq: "sparse" } }) {
    items {
      id
      meta { priority }
      refs { owner { id type slug resolved } }
      content { sections { details } }
    }
    totalCount
  }
}
