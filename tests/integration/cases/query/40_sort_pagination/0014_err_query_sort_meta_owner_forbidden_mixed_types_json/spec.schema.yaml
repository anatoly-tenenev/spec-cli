version: "0.0.4"
entity:
  feature:
    idPrefix: FEAT
    pathTemplate: "features/{slug}.md"
    meta:
      fields:
        owner:
          schema:
            type: entityRef
            refTypes: [service]
  service:
    idPrefix: SVC
    pathTemplate: "services/{slug}.md"
    meta:
      fields:
        owner:
          schema:
            type: string
